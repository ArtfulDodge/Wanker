apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'wank.Main'

run{
 standardInput = System.in
}

jar {
    manifest {
        attributes 'Main-Class': 'wank.Main'
        baseName = 'Wanker'
        version = '1.0.1'
    }
}
